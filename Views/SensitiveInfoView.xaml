<UserControl x:Class="PdfRedactionApp.Views.SensitiveInfoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PdfRedactionApp.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <DataGrid Name="dataGridSensitiveInfo" AutoGenerateColumns="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="类型" Binding="{Binding Type}" Width="100"/>
                <DataGridTextColumn Header="原始文本" Binding="{Binding OriginalText}" Width="200"/>
                <DataGridTextColumn Header="脱敏后文本" Binding="{Binding RedactedText}" Width="200"/>
                <DataGridTextColumn Header="页码" Binding="{Binding PageNumber}" Width="80"/>
                <DataGridTextColumn Header="位置" Binding="{Binding Position}" Width="150"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>